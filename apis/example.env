# SMTP / Email
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=executesoft.info@gmail.com
SMTP_PASSWORD=xnfumntpcoxczsce
SMTP_STARTTLS=true
EMAIL_FROM=executesoft.info@gmail.com
EMAIL_FROM_NAME=Execute Academy

# --- Database ---
DATABASE_URL=postgres://execute_academy:password@localhost:5432/execute_academy?sslmode=disable
DB_HOST=localhost
DB_PORT=5432
DB_USER=execute_academy
DB_PASSWORD=password
DB_NAME=execute_academy
DB_SSLMODE=disable

# --- Seed (Admin) ---
# Defaults used if not provided
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@execute_academy.local
ADMIN_PASSWORD=admin123


# --- System ---
APP_ENV=development
LOG_LEVEL=info
API_HOST=127.0.0.1
API_PORT=8080
GRPC_HOST=127.0.0.1
GRPC_PORT=50051
SHUTDOWN_GRACE_SECONDS=10

# --- Auth/JWT ---
JWT_SECRET=SECRRTT@$$$454^^!!!DF@@@222
JWT_ISSUER=execute_academy
JWT_ACCESS_TTL_SECONDS=900
JWT_REFRESH_TTL_SECONDS=2592000

# --- Social Auth (optional) ---
# Google OAuth 2.0 Client ID (to validate Google ID tokens)
GOOGLE_OAUTH_CLIENT_ID=

# GitHub OAuth App credentials (for code exchange)
GITHUB_OAUTH_CLIENT_ID=
GITHUB_OAUTH_CLIENT_SECRET=


# --- Redis ---
# If set, takes precedence over the individual REDIS_* variables
REDIS_URL=redis://127.0.0.1:6379/0
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_USERNAME=
REDIS_PASSWORD=
REDIS_DB=0
REDIS_TLS=false

# --- Kafka (Email Queue) ---
# If running API locally (outside Docker), use the host-advertised listener from compose
# bitnami/kafka exposes localhost:9094 for host access
KAFKA_BROKERS=localhost:9094
KAFKA_EMAIL_TOPIC=emails
KAFKA_CLIENT_ID=execute_academy_api

