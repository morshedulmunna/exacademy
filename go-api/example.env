# Application Configuration
VERSION=1.0.0
API_VERSION=v1

# Logging Configuration
LOG_LEVEL=info

# Application Mode: development, production, testing
MODE=development
SERVICE_NAME=execute_academy

# Server Ports
HTTP_PORT=9090
GRPC_PORT=50001

# Health Check Configuration
HEALTH_CHECK_ROUTE=/health

# JWT Configuration
JWT_SECRET=SKooLLZ@!!22
SERVICE_BASE_PATH=/api/v1

# MongoDB Configuration (either provide MONGO_URI or individual parts)
# Matches docker-compose credentials (root/password) and DB name (execute_academy)
MONGO_URI=mongodb://root:password@localhost:27017/execute_academy?authSource=admin
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_USER=root
MONGO_PASSWORD=password
MONGO_DATABASE=execute_academy
MONGO_AUTH_SOURCE=admin
MONGO_REPLICA_SET=
MONGO_TLS=false
MONGO_MAX_POOL_SIZE=20
MONGO_MIN_POOL_SIZE=0
MONGO_CONNECT_TIMEOUT_MS=10000

# Redis Cache Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=password
REDIS_DB=0
REDIS_POOL_SIZE=10
REDIS_MIN_IDLE=5

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
FROM_EMAIL=noreply@execute_academy.com
FROM_NAME=execute_academy
EMAIL_TEMPLATE_PATH=./pkg/email/templates
EMAIL_QUEUE_TYPE=kafka

# Kafka Configuration
KAFKA_ENABLE=true
KAFKA_CLIENT_ID=execute_academy
KAFKA_GROUP_ID=execute_academy-group
KAFKA_BROKERS=localhost:9092
KAFKA_TOPIC_EVENTS=email-requests
KAFKA_TOPIC_COMMANDS=user-commands
KAFKA_CONSUMER_AUTO_OFFSET_RESET=earliest
KAFKA_CONSUMER_MAX_BYTES=1048576
KAFKA_CONSUMER_MAX_WAIT_SECONDS=10
KAFKA_PRODUCER_REQUIRED_ACKS=1
KAFKA_PRODUCER_MAX_ATTEMPTS=3

# OAuth Configuration
GOOGLE_CLIENT_ID="578264402581-f0an1n8llgpa1n0bpq4cetkn8pjatqui.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="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"
# The callback URL that Google will redirect to (must match Google Console)
OAUTH_REDIRECT_URL=http://localhost:9090/api/v1/auth/google/callback
# Frontend base URL to redirect after successful login
FRONTEND_APP_URL=http://localhost:3000

# CORS
# When running frontend on localhost:3000, allow it here. Comma-separated values supported.
CORS_ORIGIN=http://localhost:3000
