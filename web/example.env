# Example env for the web app. Copy to .env.local for local development.

# Base API URL used by Next.js rewrites and browser calls to /api/*
# Local backend default
API_BASE_URL=http://localhost:8080

# Internal API URL used on the server (SSR / route handlers). In Docker, this
# usually points at the service name on the Docker network. For pure local dev
# without Docker, you can set it to the same value as API_BASE_URL.
INTERNAL_API_BASE_URL=http://api:8080

# Standard Node environment flag
NODE_ENV=development


